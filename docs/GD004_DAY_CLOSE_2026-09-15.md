# GD-004 Day Close — 2026-09-15

## End-of-day status

The ADP Workforce Now live path progressed from read-only inspection to a bounded, evidence-backed mutation flow without enabling file upload or final submit.

Verified live sequence:

1. OneTrust preference center can be opened safely.
2. `Unselect All` + `Save Changes` clears the cookie boundary under an exact reviewed fingerprint.
3. One reviewed `Apply` click opens the guest identity step.
4. First Name, Last Name, and Email can be written with exact readback.
5. A unique reviewed `Continue` action can be clicked exactly once.
6. Post-Continue diagnostics prove that the current blocker is ADP custom validation rather than navigation.

## Key live runs

- `34983959851` — preference-center read-only inspection succeeded.
- `34986082662` — preference-safe-fill succeeded: cookie policy + Apply + 3 identity writes verified.
- `34988265197` — reviewed Continue click succeeded, but page remained on the same identity stage.
- `34989901584` — post-Continue diagnostic succeeded and identified the blocking validation surface.

## Current blocker

ADP custom validation reports:

- `Invalid First Name.`
- `Invalid Last Name.`
- `Mobile Number is required.`

The three identity values passed browser-level readback and validity checks, so native HTML validity is not sufficient for this ATS. The phone control also appears structurally optional but is functionally required by ADP runtime validation.

## Safety state

Still disabled:

- credentials/social sign-in;
- phone/country mutation;
- CV/file upload;
- additional application navigation beyond the reviewed Continue;
- CAPTCHA handling/bypass;
- final Submit.

Final Submit kill switch remains engaged.

## First next step

Start with an **ADP validation-contract investigation**, not another live Continue retry.

The investigation should collect non-secret evidence for the name and mobile custom validators, including custom-element attributes/state, validation events/messages, maxlength/pattern-like metadata if exposed, and the actual runtime requiredness contract. It must remain fail-closed and must not silently transliterate or otherwise alter candidate identity data.

Decision gate after that evidence:

- If ADP requires a normalized/transliterated name representation, define a user-reviewed explicit profile field/policy; never transform the legal/candidate name silently.
- If Mobile Number is truly required, extend the dedicated ADP profile schema with phone country + mobile only after explicit approval, then build a narrowly scoped phone safe-fill canary.
- Only after the identity/phone stage advances should the project inspect the full application manifest and consider exactly one reviewed CV upload.

## Repository state at close

Main before this documentation-only day-close change: `59b46b37e538567a8e039c3fe453eb1e79c971e1`.

Relevant implementation PRs completed today include the cookie preference path, preference safe-fill, Continue canary, and post-Continue diagnostic canary. No further live workflow should be dispatched today.
